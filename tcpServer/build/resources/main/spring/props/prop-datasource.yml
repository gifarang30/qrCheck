# 데이터소스 설정 프로퍼티
datasource:

  # kumo 데이터베이스
  kumo:
    db-type: mariadb

#    jndi-name: java:comp/env/jdbc/KUMO
    jdbc-url: jdbc:mariadb://localhost:3306/kumo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: kumo
    password: kumo2022!@#
    driver-class-name: org.mariadb.jdbc.Driver